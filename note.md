```
进入指令：mysql -u face -p face_db
密码：face123
查看表格：SHOW TABLES;
查看用户：
SELECT * FROM users;
SELECT * FROM face_feature;

查看签到记录
SELECT * FROM attendance;

SELECT * FROM courses;

SELECT * FROM course_roster;

DESC attendance;
```

管理员密码（默认：admin123）